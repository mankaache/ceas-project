import {commonImages} from '@/assets'

const HeroSection = () => {
    return (
        <div className={'h-[80vh] home-slide w-full bg-center bg-cover bg-no-repeat flex justify-center items-center'} style={{backgroundImage:`url(${commonImages.Aboutus3})`}}>
            <p className={'z-[100] text-white'}>HeroSection</p>
            </div>
    )
}

export default HeroSection